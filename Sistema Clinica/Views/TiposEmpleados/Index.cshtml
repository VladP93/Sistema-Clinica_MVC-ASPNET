@model IEnumerable<Sistema_Clinica.Models.TipoEmpleado>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tem_tipo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tem_tipo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.tem_idTipoEmpleado }) |
            @Html.ActionLink("Details", "Details", new { id=item.tem_idTipoEmpleado }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.tem_idTipoEmpleado })
        </td>
    </tr>
}

</table>
