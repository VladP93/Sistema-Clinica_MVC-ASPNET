@model IEnumerable<Sistema_Clinica.Models.Especialidad>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.esp_Especialidad)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.esp_Especialidad)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.esp_idEspecialidad }) |
            @Html.ActionLink("Details", "Details", new { id=item.esp_idEspecialidad }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.esp_idEspecialidad })
        </td>
    </tr>
}

</table>
