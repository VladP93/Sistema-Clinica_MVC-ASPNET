@model IEnumerable<Sistema_Clinica.Models.Usuario>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.usu_usuario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.usu_contrasena)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Rol.rol_rol)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.usu_usuario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.usu_contrasena)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Rol.rol_rol)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.usu_idUsuario }) |
            @Html.ActionLink("Details", "Details", new { id=item.usu_idUsuario }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.usu_idUsuario })
        </td>
    </tr>
}

</table>
