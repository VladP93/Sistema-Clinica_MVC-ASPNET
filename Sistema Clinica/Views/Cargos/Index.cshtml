@model IEnumerable<Sistema_Clinica.Models.Cargo>

@{
    ViewBag.Title = "Cargos";
}

<h2>Lista de Cargos</h2>

<p>
    @Html.ActionLink("Agregar Cargo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Cargo
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.car_cargo)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.car_idCargo }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.car_idCargo }) |
            @Html.ActionLink("Borrar", "Delete", new { id=item.car_idCargo })
        </td>
    </tr>
}

</table>
